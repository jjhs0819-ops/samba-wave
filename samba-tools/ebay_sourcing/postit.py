"""[폐기·실행 금지] tcg_vault 포스트잇 Gemini 합성.

2026-07-30 사용자 결정: eBay 대표사진에 **포스트잇 합성 영구 금지**(오려붙인 티 +
가짜 배경 확장으로 반복 격노). 대표사진은 소싱처 원본에서 **크롭만** 한다.
→ 신규 경로: `fix_no_postit.py`, `postit_half.py::compose`(포스트잇 미합성)
"""

import sys

sys.exit("[폐기] 포스트잇 합성 영구 금지. fix_no_postit.py 를 쓸 것.")
